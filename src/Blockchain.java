import java.io.*;
import java.util.ArrayList;

// Make Blockchain serializable
class Blockchain implements Serializable {
    // List to hold the chain of blocks.
    // Difficulty level for mining.
    }
    // Adds a new block to the blockchain after mining it.
    public void addBlock(Block newBlock) {

    }
    // Check if the blockchain is valid
    public boolean isChainValid() {
        // Loop through blockchain to check hashes.
        // Compare registered hash and recalculated hash for current block.
        // Compare previous block's hash to the registered previous hash in the current block.
    }
}